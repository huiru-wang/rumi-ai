# TODO

## 功能待办

### 通用功能
- [ ] 前端播放器组件优化：增加字幕（字幕需要切分并有timeline）。每页PPT间隔太短，体验不好，应该增加至少1s的空间隔（可以是工程上实现，在前端增加播放间隔）
- [ ] 增加一个种子数据workspace，新建账号时，自动初始化到对应的用户下（包括历史会话记录、task产物等等），可以自行删除。
- [ ] web_search支持
- [ ] 用户问题反馈入口
- [ ] langgraph的state管理切换到自主数据库backend/.langgraph_api/
- [ ] 管理后台userId-邀请码数据

### PPT
- [ ] P0: 增加耗时预计
- [ ] P0: PPT保存时，直接保存到当前的文件内，而不是下载一个新的。
- [ ] 改造PPT的结构为按页结构，修改时可以针对某页进行单独改造。（涉及PPT模版、任务提供更新Tool）
- [ ] PPT SKILL的产物效果：信息收集、素材规划、内容质量，还需优化
- [ ] PPT SKILL对于用户自定义模版的产物效果、还原度效果等；
- [ ] PPTX风格提取效果：后续需要增加企业图标
- [ ] PPTX风格提取：后续给单页理解增加真实 slide PNG / 截图输入，提升复杂母版、裁剪、阴影、组合图形的判断准确性。
- [ ] PPTX风格提取：后续将逐页 LLM 调用从单线程升级为可配置并发，以优化大页数 PPTX 的耗时。
- [ ] PPTX风格提取：后续扩展 `resource_manifest`，支持普通内容图片、企业 Logo、图标、纹理和装饰图形；首版只纳入背景图片。
- [ ] TTS优化：当前TTS使用qwen3-tts-flash，中英掺杂场景效果不好，英文读取来很难受，时不时还冒出来日语。考虑切换cosyvoice-v3-flash，稳定性很好，但是生成耗时是前者的2倍多，成本也更高。
- [ ] 导出PPTX
- [ ] 增加图片插入能力（已有将文档的图片插入ppt）

### 体验问题

3. [ ] 新产出完成时增加气泡提示（如"xxx 生成完成，可预览/编辑/生成口播稿"）
4. [ ] 发送新消息后，自动滚动将用户消息滚到视窗顶部
6. [ ] 引用角标渲染不稳定，有时无法显示章节-标题-页；需考虑 fallback（文档可能无标题/切分缺少标志）

## 边界限制

- [x] 每个用户限制5个工作区
- [x] 每个工作区的知识库限制5个文档。
- [x] 每个工作区的产出限制生成10个PPT任务、10个自定义风格任务
- [x] 每个PPT任务下限制生成5个口播稿
- [x] 每个用户限制5个自定义风格 

超出限制根据场景提示用户删除不再需要的任务或者文档。

## 稳定性
- [ ] 断线重连机制

## 架构与决策

1. [ ] 上下文管理优化（token 成本、注意力效果、压缩频率之间的权衡）
2. [ ] 消息渲染方案（历史消息 + 实时 SSE 合并渲染）
3. [ ] 清晰架构（通用 Agent + SKILL 注入业务 + 定制 Tools，业务与 Agent 能力分离）
4. [ ] 会话消息加载性能优化（tool_result 延迟加载）
5. [ ] LangGraph 能力边界评估（提供了什么？缺什么？）

### 上下文管理优化

> 当前压缩太频繁，会话内容较多时几乎每次用户消息都会触发 summary。

- [ ] System Prompt 永不压缩
- [ ] 工具结果在压缩时清空
    - ppt技能和reference很占用上下文，在ppt生成完成闭环后，应该将load_skill中的输出清空或替换为「再次执行工具读取」等提示。
    - save_ppt：可清空输出的文件内容或替换为其他总结。
    - save_narration：可清空或替换为其他总结。
- [ ] UI 优化：上下文压缩存在卡顿。应该有提示或者移步压缩？

## 迭代
- [ ] 登录认证
- [ ] 成本模块：token统计
- [ ] 数据后台：用户活跃度、用户维度token统计、用户工作区维度token统计、用户文档统计数量、大小；用户反馈统计（产品交互上增加反馈交互：点赞、点踩等）
